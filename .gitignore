pipeline_runs.json
__pycache__/
*.pyc
*.pyo
.streamlit/
.env
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfvars
core/venv/
venv/
.venv/

# Raw datasets: NEVER git-tracked (GitHub 100MB limit; 2026-07-27 incident).
# They live on the 500GB disk via the data/raw symlink; data/MANIFEST.md SHA256s are
# the integrity record and scripts/fetch_data.py reproduces them.
data/raw
data/raw/**

# Third-party clones + agent working dirs (lya-mfdm has NO LICENSE - never redistribute via this repo)
phase1_work/

# AlphaEvolve datalake mirror: mutable GCS sync target, regenerated by scripts/datalake_harvest.py
data/datalake_checkpoints/

# audit_datalake.py resumption cache (low-tier queue A-S3-2, 2026-08-01):
# private, regenerable, purely a between-invocation resume point — the real
# deliverable is data/GCS_DATALAKE_MANIFEST.md, which stays tracked.
data/.audit_datalake_checkpoint.json
