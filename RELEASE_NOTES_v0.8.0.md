# Release v0.8.0 — WP-A Adjudication, Off-Ramp 3 Terminus, Dual-Scale Brief

**Date:** 2026-07-25
**Scope:** empirical-confrontation process work only. No observational data is fetched or
compared; milestone gate **G1-L remains closed** (`pipeline/gate.py` logic unchanged).
Nothing in this release is a physical or empirical claim — it is the record of a
two-model-adjudicated negative result, one code-quality fix, and an analysis brief.

---

## What this is

Continuation of the swampland-bounds pivot (`v0.7.0` era work) through to its honest
conclusion, plus scoping for a possible next direction.

### 1. WP-A adjudicated — two-model dispute resolved

Deep Think (T0s) filed a blind re-derivation of the Off-Ramp 2 swampland bounds
(`SWAMPLAND_BOUNDS_A123.md`). Adjudicated in `briefs/T0_ADJUDICATION_WPA_2026_07_25.md`:
concurrence on every content item; one genuine delta (chameleon scale-bridge) resolved in
Deep Think's favor — gap G-1 upgraded to **CLOSED-NEGATIVE**: under the pivot's own
mass-anchoring assumption, no Mpc-scale observable can test the derived window, in
principle. Pivot ruling Steps 5.1/5.4 voided; the D-3 Phase 2 GPU rerun authorization is
suspended (banner on `STREAM3_AUTHORIZATION_SIGN_OFF_2026_07_25.md`, text preserved).

### 2. WP-A2 circularity audit — FAIL — Off-Ramp 3 terminus

The one surviving continuation (a laboratory-scale re-scope onto public inverse-square-law
data) was audited (`WP_A2_CIRCULARITY_AUDIT.md`, machine-checked by
`scripts/verify_wpa2_circularity.py`, executed, citations fetch-verified: arXiv:2002.11761,
hep-ph/0502025). Result: **FAIL** — no region of the derived window is both non-circular
and reachable by any published public dataset at gravitational strength. Recorded in
`NO_PREDICTION_BRANCH.md` §8.5: the [A-DD]-anchored hypothesis is untestable at every scale
with data that exists today. A clean negative result, reported with full prominence, not a
process failure. One monitoring trigger (F-LAB) remains open for future public data.

### 3. WP-E code-quality pass

Exact cubical-complex Euler-characteristic computation for the Betti-number observable
(replacing a prior heuristic) was reviewed; one test assertion tightened from a tolerance
check to exact-match, consistent with the computation's actual guarantees. 136 pipeline
tests green; `check_tier_language.py` clean throughout.

### 4. Dual-Scale (Stream 1) experimentation brief

Requested analysis of real data holdings and a possible next hypothesis
(`DUAL_SCALE_EXPERIMENTATION_BRIEF_2026_07_25.md`). Findings: the candidate "new" theory is
Stream 1 of this same three-repo project, reusing the identical cooper_s7/s10 geometry that
Off-Ramp 3 just closed, reframed via a chameleon mediator instead of the Dark Dimension
scenario — no frozen prediction exists there yet. Real-data inventory: NANOGrav 15yr
pulsar timing is the only dataset on disk with genuine public provenance; no real Euclid
survey data exists on disk; SDSS holdings are unmanifested. Three previously-unaudited
scripts (`real_euclid_worker.py`, `ned_cross_validator.py`, `process_nanograv.py`) were
found to contain live fabrication defects matching this project's own documented Lesson L5
and were quarantined with in-file banners (not deleted). The brief requests a Deep Think
blind derivation on chameleon-scale coherence before any pivot ruling — no experiment has
started.

## Status vs. G1/G1-L

Unchanged: **G1 open** (mechanical pin exists), **G1-L closed** (no derivable, testable
target survives). No new PREDICTION.md version. No data fetched.

## Commits in this release

- `8efab40` WP-E: exact cubical-complex Euler characteristic for Betti numbers
- `89d96e5` WP-A adjudicated: blind pass resolved, G1-L remains closed, pivot re-scoped
- `0e3ddeb` WP-A2 Gate 0 FAIL: circularity audit -> Off-Ramp 3 terminus for the pivot
- `0cc645a` test: tighten Euler-characteristic check to exact match
- `e89ccf1` Brief: Dual-Scale (Stream 1) experimentation options for Deep Think + Xavier signoff

---

`Generated-by: Fable 5 (T0-delegated) | Verified-by: pipeline/tests/ (136 passed), scripts/check_tier_language.py (0 violations), scripts/verify_wpa2_circularity.py (executed) | Reviewed-by: T0 Y (this release compiles adjudications already reviewed in-line; Xavier countermand window open per each cited document)`
