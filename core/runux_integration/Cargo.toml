[package]
name = "runux_tda_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
runux-ai-runtime = { path = "../runux-ai-runtime" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
