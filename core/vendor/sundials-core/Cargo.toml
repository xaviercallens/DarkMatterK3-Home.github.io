[package]
name = "sundials-core"
version = "0.1.0"
edition = "2021"
description = "Core types for rusty-SUNDIALS: Real, Context, error handling"

[dependencies]
rayon = { version = "1.8", optional = true }

[features]
experimental = []
regenerate-bindings = []
parallel = ["rayon"]

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "core_bench"
harness = false
