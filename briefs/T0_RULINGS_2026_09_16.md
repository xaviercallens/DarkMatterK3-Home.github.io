# T0 Rulings — 2026-09-16

**Ruled by:** T0 (Xavier Callens), in session 2026-09-16, responding to the 2026-09-16
status analysis of Track A (experimentation) and Track B (sandbox). **Recorded by:** Claude
(Opus 5). Track B rulings on S2 material are mirrored in the S2 repo (see §B).

---

## A. Track A (this repo)

### A1. SWEEP 66→9 aggregation — Option 1 APPROVED
Recorded in full at the end of `briefs/T0_DECISION_REQUEST_SWEEP_AGGREGATION_2026_07_31.md`.
Implementation owed, not started.

### A2. Aug 1 low-tier queue — no action
Planck plik_lite stays staged in the bucket, outside `data/raw/`, until a Planck channel
amendment is pinned (DL-3, unchanged).

### A3. Harvest daemon — linger + code fixes
- **Linger:** enabled 2026-09-16 on T0 direction (`loginctl show-user` → `Linger=yes`). A-S3-1
  (2026-08-01) had shipped the systemd unit without it; the unit therefore stopped with each
  login session. Correction note added to `briefs/LOW_TIER_QUEUE_2026_08_01.md`.
- **Duplicate log lines — fixed.** Cause found: the daemon logged to `LOG_FILE` *and* to
  stdout, while the unit appends stdout to the same `LOG_FILE`. The stdout handler is now
  attached only on a TTY. Verified after restart: one line per event.
- **Temp-directory hardening — not applied, no code to patch.** Neither
  `scripts/datalake_harvest.py` nor the daemon creates temp files. The 2026-09-16
  05:38–07:38 UTC failures are raised inside `gsutil`'s own bundled Python
  (`multiprocessing` → `tempfile`: "No usable temporary directory found"), plus
  `[Errno 28] No space left on device` on the heartbeat check — i.e. at least one filesystem
  the harvest touches was full. After the 18:42 reboot, `/` is at 29% and `/tmp` holds 608K. **What filled the disk
  is not established:** no optimizer runs on this VM (AlphaEvolve checkpoints are synced
  from GCS), and a grep of the previous boot's journal returned nothing (it may not be
  fully readable by this user). Open item — if it
  recurs, capture `du -xh --max-depth=2 /` *before* rebooting.

### A4. AlphaEvolve harvest figures on `main` — sandbox ruling supersedes Directive 1
- **Ruling:** the 07-31 sandbox ruling (DL-3; CLAUDE.md ledger item 5) supersedes T0
  Directive 1 (2026-07-30) **for the `main` branch**. Future AlphaEvolve convergence figures
  are not refreshed on `main`.
- **This commit series:** the three pending `figures/live/*` files (generation 75 → 300,
  run `20260729_074350`) are committed once, per T0 instruction, as the final `main`
  snapshot, with the harvest gaps disclosed.
- **Harvest gaps (monitoring, not data):** the daemon log shows harvests only on 07-31,
  08-01/02, 08-23, 08-27/28, 09-10, 09-12, 09-15/16. Each harvest is a full
  `gsutil rsync` of the GCS checkpoint prefix, so the figures are rebuilt from all 304
  checkpoints — the gaps are periods with no refreshed figure, not missing generations.
- **Implemented 2026-09-16 (T0 confirmed "continue following your recommendations").**
  Destination is the orphan branch **`experimental/stream4-alphaevolve-diagnostics`**, not
  the `track-a-diagnostics` name first given (AlphaEvolve is Stream-4 material, not Track A)
  and not the `sandbox/alphaevolve-diagnostics` name first recommended here: that
  recommendation cited ledger item 7, but AlphaEvolve falls under item 5, whose only branch
  precedent is `experimental/stream4-cag-poc`. Undated because the branch is a rolling
  snapshot target.
- **Mechanism:** `scripts/datalake_harvest.py` still writes to `figures/live/` (unchanged,
  so the heartbeat path in `briefs/EXECUTION_PLAN_2026_07_31_DATALAKE_GOVERNANCE.md` stays
  valid). `figures/live/` is gitignored on `main` and its three files were removed from
  `main`'s tree one commit after the `11b6477` snapshot. After each successful harvest, the
  daemon copies the files into a worktree of the diagnostics branch
  (`../DarkMatterK3-Home-stream4-diagnostics`, overridable via
  `STREAM4_DIAGNOSTICS_WORKTREE`) and commits only when run/checkpoint/generation/best
  candidate changes.
- **Push limitation:** push auth in interactive sessions comes from `GH_TOKEN` via `gh`,
  which the systemd service does not have. The daemon keeps its commits locally and logs a
  warning when a push fails. Pushing unattended needs a stored credential for the service
  (e.g. `gh auth login`); the token was not copied into the unit file.
---

## B. Track B (S2 repo, sandbox — all non-citable)

- **B1. LR-3 survivor A079727 — HOLD G1-3/G1-4.** No mirror-integrality or monodromy runs on
  A079727 until it is analytically checked for being a partial-sum / twist of a known
  hypergeometric family rather than new geometry. (The other two survivors were not ruled
  on.) Recorded on `sandbox/autoevolve-r2-phase-a-2026-08-01`.
- **B2. M24/Mukai criterion proposal — Option 3, HOLD; returned for corrections.** Checker
  not authorized. Corrections required before resubmission: (i) a symplectic group fixes the
  holomorphic 2-form and acts trivially on T, so the §2 "embeds into O(T)" framing is
  backwards — the lattice condition belongs on the coinvariant lattice inside NS(X);
  (ii) a bare symplectic-involution test cannot separate the two ρ = 19 candidates (T0: both
  admit Shioda–Inose structures); (iii) the §2 table uses the T for cooper_s7 from a DRAFT
  certificate that is exactly the open U1 item. Recorded on
  `sandbox/m24-moonshine-criterion-2026-09-16`.
- **B3. External K3×T²/M24 moonshine document — REJECTED.** Grounds recorded in the register
  (fabricated/misattributed OEIS IDs; Kummer node count 16, not 24; M24 is simple, so no
  A4 quotient). The document itself was never committed to any repo, so there is nothing to
  purge; the register is kept as the rejection record. Recorded on S2 `main`.

---

Generated-by: Claude (Opus 5) | Verified-by: daemon log/journal/`loginctl` read directly;
restart verified single-line logging | Reviewed-by: T0 Y (rulings given in session; A4 branch
confirmed 2026-09-16)
